import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// import { AIcon } from '@Aerolii-UI/components'

// createApp(App).use(AIcon).mount('#app')
createApp(App).mount('#app')
